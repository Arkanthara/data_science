If rate less or equal to entropy, some distortions are created => lossy compression
To have lossless compression, need a bit rate greater or equal to entropy

If symbols happens very very ofen, I assign a few bits and if symbols happens only sometimes, I assign more bits...

Compress parameters of LLM models -> work properly on smaller machines !!!

Use compression of parameters for training ???

Compress after else problems with gradients and hallucinations
